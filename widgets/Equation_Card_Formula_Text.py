import flet as ft

def Equation_Card_Formula_Text(text, color="#047648", weight=ft.FontWeight.W_600, size=16):
     return ft.Text(text, 
                        size= size, 
                        weight=weight, 
                        color= color)